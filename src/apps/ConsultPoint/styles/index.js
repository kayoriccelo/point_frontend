import { makeStyles } from '@material-ui/core/styles';


const useStyles = makeStyles({
    rootHeader: {
        marginBottom: 15
    },
    paperHeader: {
        display: 'flex',
        alignItems: 'center',
        width: '100%',
    },
});

export default useStyles;