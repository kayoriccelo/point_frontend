import { makeStyles } from '@material-ui/core/styles';


const useStyles = makeStyles({
    input: {
        marginBottom: 20,
        width: '70%'
    },
    button: {
        width: '70%'
    }
});

export default useStyles;
